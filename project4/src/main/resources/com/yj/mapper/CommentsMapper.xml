<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yj.mapper.CommentsMapper">
	<insert id="insert">

		insert into comments (commentkey, userkey,
		commcontents, commdate)
		values (seq_reply.nextval, #{userkey},
		#{commcontents}, sysdate)

	</insert>

	<select id="read" resultType="com.yj.domain.CommentsVO">

		select * from comments where
		commentkey =
		#{commentkey}

	</select>


	<delete id="delete">

		delete from comments where commentkey =
		#{commentkey}

	</delete>

	<update id="update">

		update comments set commcontents =
		#{commcontents},commdate =
		sysdate
		where commentkey =
		#{commentkey}

	</update>

</mapper>