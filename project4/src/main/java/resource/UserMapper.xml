<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="project4.mapper.UserMapper">

	<select id="list" resultType="project4.domain.UserVO">
		select * from users
	</select>

	<select id="get" resultType="project4.domain.UserVO"
		parameterType="int">
		select *
		from
		users
		where userkey=#{userkey}
	</select>

	<insert id="insert">
		INSERT INTO users
		values(seq_users.nextval, #{name},
		#{nickname}, #{id},#{password},
		#{phonenum}, sysdate, #{loc}, #{grade})
	</insert>

	<update id="update" parameterType="project4.domain.UserVO">
		update users
		set
		password=#{password},
		nickname=#{nickname},
		name=#{name},
		loc=#{loc},
		phonenum=#{phonenum}
		where userkey=#{userkey}
	</update>

	<delete id="delete" parameterType="int">
		delete
		from users
		where
		userkey=#{userkey}
	</delete>

	<select id="logincheck" parameterType="String"
		resultType="String">
		select name
		from users
		where
		id=#{id}
		and password=#{password}
	</select>

	<select id="idCheck" resultType="int">

		select count(*) from users where id=#{id}

	</select>
	
	<select id="login" parameterType="project4.domain.UserVO" resultType="project4.domain.UserVO">
		select * 
		from users
		where id=#{id} and password=#{password}
	</select>	

</mapper>